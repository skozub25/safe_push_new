# Env-style corpus for SafePush

# SECRET via provider pattern (AWS access key ID)
AWS_ACCESS_KEY_ID="AKIA1234567890ABCDE1"  # safepush: EXPECT_SECRET pattern

# SECRET via high-entropy value in sensitive context (no provider pattern here)
AWS_SECRET_ACCESS_KEY="Z7xY6wV5uT4sR3qP2oN1mL0kJ9"  # safepush: EXPECT_SECRET entropy

# SECRET via Stripe live secret key
STRIPE_SECRET="sk_live_1234567890abcdefghijklmn"  # safepush: EXPECT_SECRET pattern

# SAFE: looks Stripe-ish but too short to match our provider pattern
STRIPE_SECRET_TEST="sk_live_demo"  # safepush: EXPECT_SAFE pattern

# SAFE: session-like identifier, allowed as LOW / non-blocking
SESSION_ID="abc123def456ghi789"  # safepush: EXPECT_SAFE entropy

# SAFE: ordinary configuration values
PORT=8080  # safepush: EXPECT_SAFE entropy
ENVIRONMENT="development"  # safepush: EXPECT_SAFE entropy

# SAFE: URL with query params (long-ish but not a secret)
CALLBACK_URL="https://example.com/callback?state=abc123"  # safepush: EXPECT_SAFE entropy

# SECRET: long opaque token with no explicit provider pattern
OPAQUE_TOKEN="Q1w2E3r4T5y6U7i8O9p0AaBbCcDdEe"  # safepush: EXPECT_SECRET entropy
